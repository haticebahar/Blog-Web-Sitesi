@model BlogSitesi.Models.Kategori

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="col-md-8">
    <h2 style="padding-top:50px;font-weight:bolder;color:red">Delete Screne</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4 style="font-weight:bold ; color:black ">Category</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.kategoriAdi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.kategoriAdi)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn-light" style="color:blue ;font-weight:bolder" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</div>
