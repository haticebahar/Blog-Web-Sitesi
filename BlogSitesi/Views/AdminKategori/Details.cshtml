@model BlogSitesi.Models.Kategori

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="col-md-8">
    <h2 style="padding-top:50px;font-weight:bolder;color:red">Details</h2>

    <div>
        <h4 style="font-weight:bold ; color:black ">Category</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.kategoriAdi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.kategoriAdi)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.kategoriId }) |
        @Html.ActionLink("Back to List", "Index")
    </p>

</div>